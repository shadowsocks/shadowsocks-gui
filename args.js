// Generated by CoffeeScript 1.6.3
(function() {
  var deleteConfig, loadConfig, loadProfiles, saveProfiles;

  loadProfiles = function() {
    var configs, e;
    if (localStorage['config']) {
      try {
        return configs = JSON.parse(localStorage['config']);
      } catch (_error) {
        e = _error;
      }
    } else {
      return {};
    }
  };

  saveProfiles = function(profiles) {
    return localStorage['config'] = JSON.stringify(profiles);
  };

  loadConfig = function(profile) {
    var configs, e;
    if (localStorage['config']) {
      try {
        return configs = JSON.parse(localStorage['config']);
      } catch (_error) {
        e = _error;
      }
    }
  };

  deleteConfig = function(profile) {
    var profiles;
    profiles = loadProfiles();
    if (profiles[profile]) {
      delete profiles[profile];
    }
    return saveProfiles(profiles);
  };

}).call(this);
